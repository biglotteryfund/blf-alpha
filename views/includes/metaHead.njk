<link rel="stylesheet" type="text/css" href="/assets/build/{{ ASSETS_VERSION }}/stylesheets/style.css">
<link rel="stylesheet" href="https://use.typekit.net/isx5yfw.css">

<script id="script-config">
(function() {
    window.AppConfig = {
        environment: '{{ appData.environment }}',
        isTestServer: {% if appData.isTestServer %}true{% else %}false{% endif %},
        locale: '{{ locale }}',
        localePrefix: '{{ localePrefix }}',
        sessionExpirySeconds: {{ appData.config.get('session.expiryInSeconds') }},
        authCookieName: '{{ appData.config.get('session.cookieLogin') }}'
    };

    var docEl = document.documentElement;
    docEl.className = docEl.className.replace('no-js', 'js-on');

    if (
        navigator.userAgent.indexOf('MSIE') !== -1 ||
        navigator.appVersion.indexOf('Trident/') > 0
    ) {
        docEl.className += ' ' + 'is-ie';
    }

    if (sessionStorage.fontsLoaded) {
        docEl.className += ' ' + 'fonts-loaded';
    }

})();
</script>
<script src="/assets/build/{{ ASSETS_VERSION }}/javascripts/head.js" id="script-head"></script>
