{% from "../components/nav.njk" import navlinks with context %}
{% from "../components/surveys.njk" import showSurvey with context %}

{# show a microsurvey for this page (if set) #}
{{ showSurvey() }}

<header role="banner">
    <div class="inner header__inner">
        <div class="grid grid--wide-only">
            <a href="{{ buildUrl('global', '/') }}"><h1 class="logo"><span class="visually-hidden">{{ __("global.brand.title") }}</span></h1></a>
            <nav class="mobile-only header__hamburger" id="js-mobile-nav-toggle">
                <a href="#nav">
                    {% include "../includes/svg/hamburger.svg" %}
                </a>
            </nav>
            <nav role="navigation" class="desktop-only grid__item">
                <ul class="nav grid">
                    {{ navlinks('nav__link') }}
                </ul>
            </nav>
        </div>
    </div>
</header>
