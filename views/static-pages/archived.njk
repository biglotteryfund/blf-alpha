{% extends "layouts/main.njk" %}
{% from "components/content-box/macro.njk" import contentBox %}
{% from "components/hero-image/macro.njk" import heroImage with context %}
{% from "components/page-title/macro.njk" import pageTitle %}

{% block extraHead %}
    <meta http-equiv="refresh" content="2;url={{ archiveUrl }}" />
{% endblock %}

{% block content %}
    {{ heroImage(fallbackHeroImage) }}

    <main class="u-nudge-up">
        {{ pageTitle(title) }}

        {% call contentBox(pageAccent) %}
            <h2>This page has been archived</h2>

            <p><strong>Automatically redirecting you to the National Archives in two seconds…</strong></p>
            <p>If you are not redirected automatically, please use this link: <a href="{{ archiveUrl }}">{{ archiveUrl }}</a></p>
        {% endcall %}
    </main>
{% endblock %}
