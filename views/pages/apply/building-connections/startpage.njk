{% from "components/buttons.njk" import startButton, printButton %}

{% extends "layouts/main.njk" %}
{% set bodyClass = 'has-static-header' %}
{% set pageAccent = 'pink' %}

{% block content %}
    <main role="main" id="content">
        <div class="content-box u-inner-wide-only accent--{{ pageAccent }} a--border-top">
            <h1>{{ form.title }}</h1>

            <div class="s-prose accent-content--{{ pageAccent }} u-constrained-wide">
                <p>
                    Funding of between £30,000 and £100,000 to help individuals,
                    communities and local services connect in a variety of ways.
                    The Fund will do this by supporting promising projects that already are,
                    or with additional support could, prevent or reduce loneliness.
                </p>

                {{ startButton(startUrl, label="Start your application") }}

                <p><a href="#questions">Preview the questions you'll be asked</a></p>

                <div class="u-tone-background-tint padded">
                    <h2>Other ways to apply</h2>
                    <p>
                        We’re available Monday–Friday from 9am to 5pm to answer questions about applying.
                    </p>
                    <ul>
                        <li><strong>Telephone</strong>: 0345 4 10 20 30</li>
                        <li><strong>Textphone</strong>: 18001 plus 0345 4 10 20 30 (for those with a hearing or speech impairment)</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="content-box u-inner-wide-only accent--{{ pageAccent }} a--border-top" id="questions">
            <div class="u-constrained-content s-prose spaced--l accent-content--{{ pageAccent }}">
                <h2>Questions you'll be asked</h2>

                <p>{{ printButton('Print these questions') }}</p>

                {% for step in form.getSteps() %}
                    <div class="step-preview accent-content--{{ pageAccent }} s-prose u-constrained-wide">
                        <h3 class="step-preview__title">{{ loop.index }}. {{ step.name }}</h3>
                        {% for fieldset in step.fieldsets %}
                            <div class="step-preview__fieldset">
                                {% if fieldset.introduction  %}
                                    <div class="step-preview__fieldset-intro">{{ fieldset.introduction | safe }}</div>
                                {% endif %}
                                {% for field in fieldset.fields %}
                                    <div class="step-preview__field">
                                        <p class="step-preview__field-label">{{ field.label }}</p>
                                        {% if field.explanation %}
                                            <div class="step-preview__field-guidance">
                                                {{ field.explanation | safe }}
                                            </div>
                                        {% endif %}
                                    </div>
                                {% endfor %}
                            </div>
                        {% endfor %}
                    </div>
                {% endfor %}

                {{ startButton(startUrl, label="Start your application") }}

                <h2>After your application is submitted</h2>
                <p>
                    This information will be reviewed by a team of Big Lottery Fund staff
                    which will decide whether the idea is something we can fund.
                    If your idea is accepted, we'll ask you to submit a full proposal.
                </p>
            </div>
        </div>
    </main>
{% endblock %}
