{% from "components/hero.njk" import hero %}
{% from "components/breadcrumb-trail/macro.njk" import breadcrumbTrail %}
{% from "components/section-links/macro.njk" import sectionLinks %}
{% from "components/caseStudies.njk" import caseStudyCollection with context  %}

{% extends "layouts/main.njk" %}

{% block content %}
    {% set pageAccent = 'pink' %}

    <main role="main">
        {{ hero(
            titleText = copy.title,
            image = heroImage,
            accent = pageAccent
        ) }}

        <div class="nudge-up">
            <div class="content-box u-inner-wide-only accent--{{ pageAccent }} a--border-top">
                {{ breadcrumbTrail(breadcrumbs) }}

                <div class="s-prose accent-content--{{ pageAccent }}">
                    <p>{{ copy.intro }}</p>

                    <ul>
                        {% for item in copy.ideas %}
                            <li>{{ item }}</li>
                        {% endfor %}
                    </ul>

                    <h2>{{ copy.callToAction }}</h2>
                </div>

                {{ sectionLinks([{
                    "label": copy.locations.england,
                    "url": localify('/funding/programmes/national-lottery-awards-for-all-england')
                }, {
                    "label": copy.locations.scotland,
                    "href": localify('/funding/programmes/national-lottery-awards-for-all-scotland')
                }, {
                    "label": copy.locations.wales,
                    "href": localify('/funding/programmes/national-lottery-awards-for-all-wales')
                }, {
                    "label": copy.locations.northernIreland,
                    "href": localify('/funding/programmes/awards-for-all-northern-ireland')
                }]) }}

                <div class="s-prose accent-content--{{ pageAccent }}">
                    <p>{{ copy.ukWideInfo | safe }}</p>
                </div>
            </div>

            {# Case Studies #}
            {{ caseStudyCollection(
                caseStudies = caseStudies,
                customSectionTitle = copy.projectExamples,
                accent = pageAccent
            ) }}

            <div class="u-inner u-align-center u-margin-bottom-l">
                <a href="{{ localify('/funding/past-grants') }}" class="btn btn--small btn--outline accent--{{ pageAccent }} a--btn">{{ copy.browseAll }}</a>
            </div>
        </div>
    </main>
{% endblock %}
