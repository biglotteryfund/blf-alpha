{% from "components/styleguide.njk" import sgExample, sgUsage %}
{% from "./macro.njk" import formHeader with context %}

{% call sgExample() %}
    {{ formHeader(
        title = 'Reaching Communities & Partnerships',
        prefix = __('global.misc.stepProgress', 1, 3),
        suffix = 'Apply for a grant over £10,000',
        backUrl = '/'
    ) }}
{% endcall %}

{% call sgUsage() %}
    As the name suggests the form header pattern appears at the top of application forms. At a minimum it requires a title and can optionally display a prefix (typically used for a step counter), suffix, and a back url.
{% endcall %}
