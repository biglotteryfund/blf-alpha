{% import "components/headers.njk" as headers %}

{% macro segment(title, text, color, imagePath, id, imageIsAbsolute = false) %}
    <div class="segment accent--{{ color }} a--border-top accent-content--{{ color }}">
        {% if imagePath %}
            <div class="segment__media">
                <img src="{% if imageIsAbsolute %}{{ imagePath }}{% else %}{{ imagePath | getImagePath }}{% endif %}" alt="" />
                {{ headers.overlayText(
                    tag = 'h2',
                    text = title,
                    additionalClasses = 't4 segment__title',
                    id = id
                )}}
            </div>
        {% endif %}
        <div class="segment__aside s-prose s-prose--long">
            {% if not imagePath %}
                <h2 class="t1" id="{{ id }}">{{ title }}</h2>
            {% endif %}
            {{ text | safe }}
        </div>
    </div>
{% endmacro %}
