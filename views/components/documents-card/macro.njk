{% macro documentsCard(title, documents, accent = 'pink') %}
    <div class="card u-accent-border-top-{{ accent }}">
        <h3 class="card__title">{{ title }}</h3>
        <div class="card__body">
            {% for document in documents %}
                <div class="document-summary">
                    <a class="document-summary__link" href="{{ document.url }}">
                        {{ document.title }}
                        {% if document.filetype or document.filesize %}
                            <small>({{ document.filetype }} {{ document.filesize }})</small>
                        {% endif %}
                    </a>
                    {% if document.contents %}
                        <ul class="document-summary__contents">
                            {% for item in document.contents %}
                                <li>{{ item }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            {% endfor %}
        </div>
        {# @TODO: Support "Download All" link #}
        {% if documents.length === 1 %}
            {% set firstDocument = documents | first %}
            <div class="card__meta u-align-center">
                <a href="{{ firstDocument }}" class="btn btn--medium">{{ __('global.misc.download') | title }}</a>
            </div>
        {% endif %}
    </div>

{% endmacro %}
